// Template for WiFi credentials. Copy this file to `wifi_credentials.h`
// (same directory) and fill in your real SSID + password. The real file
// is gitignored so credentials never get committed.

#pragma once

static const char* WIFI_SSID     = "YourSSIDHere";
static const char* WIFI_PASSWORD = "YourPasswordHere";
